<% include ../partials/header %>
<h1 class="text-center">Report Products Top 50</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Total Price</th>
            </tr>
        </thead>
        <tbody id="myTable">
            <% product_report.forEach(function(product_report){%>
                <tr>
                    <td scope="row">
                        <%=product_report.title %>
                    </td>
                    <td> 
                        <%=product_report.sum %>
                    </td>
                </tr>
                <%}); %>
        </tbody>
    </table>
    <script>
      $(document).ready(function(){
        $("#myInput").on("keyup", function() {
         var value = $(this).val().toLowerCase();
         $("#myTable tr").filter(function() {
            $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
         });
       });
    });
</script>
<% include ../partials/footer %>