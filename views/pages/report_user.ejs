<% include ../partials/header %>
<h1 class="text-center"><%=reportu[0].email %></h1>
    <table class="table">
        <thead>
            <tr>
               
                <th>Title</th>
                <th>Price</th>
               
            </tr>
        </thead>
        <tbody id="myTable">
            <% reportu.forEach(function(reportu){%>
                <tr>
                    <td scope="row">
                        <%=reportu.title %>
                    </td>
                    <td> 
                        <%=reportu.price %>
                    </td>
                   
                  
                </tr>
                <%}); %>
        </tbody>
    </table>
    <script>
      $(document).ready(function(){
        $("#myInput").on("keyup", function() {
         var value = $(this).val().toLowerCase();
         $("#myTable tr").filter(function() {
            $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
         });
       });
    });
</script>
<% include ../partials/footer %>